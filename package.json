{
  "name": "sls-aws-python-36",
  "description": "",
  "version": "0.0.1",
  "dependencies": {},
  "devDependencies": {
    "serverless-iam-roles-per-function": "ServerlessOpsIO/serverless-iam-roles-per-function#shorten-name",
    "serverless-python-requirements": "4.1.1"
  },
  "scripts": {
    "setup": "pyenv virtualenv 3.6 $(basename $(pwd)) && pyenv local $(basename $(pwd)) && pip install -r requirements.txt -r requirements-dev.txt"
  }
}
